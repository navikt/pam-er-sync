apiVersion: nais.io/v1
kind: Naisjob
metadata:
  labels:
    team: teampam
  name: pam-er-sync-dev-underenheter
  namespace: teampam
spec:
  image: {{ image }}
  schedule: "0 5 * * *" # GMT
  activeDeadlineSeconds: 60

  openSearch:
    access: write
    instance: enhetsregisteret
