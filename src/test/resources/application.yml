server:
  port: 9012
config:
  cors:
    allowCredentials: true
spring:
  profiles:
    active: test
  datasource:
    url: jdbc:h2:mem:testdb;Mode=PostgreSQL
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        current_session_contextclass: org.springframework.orm.hibernate5.SpringSessionContext
  batch:
    job:
      enabled: false
    jdbc:
      initialize-schema: always
  main:
    allow-bean-definition-overriding: true
  sql:
    init:
      continue-on-error: true
      platform: h2

opensearch:
  url: ${STILLINGESBACKEND_URL:https://pamsok-opensearch.nais.oera-q.local}

pam:
  http:
      proxy:
        url: http://155.55.60.117:8088
        enabled: true

# Management endpoints for NAIS.
management:
  endpoints:
    access:
      default: none
  metrics:
      enable:
        jvm: false
        process: false
